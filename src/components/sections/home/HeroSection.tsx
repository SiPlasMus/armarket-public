'use client';
// TODO: Implement in home page phase
// Planned content: headline, subtitle, CTA buttons, animated background, parallax elements

export default function HeroSection() {
  return (
    <section className="relative min-h-[88vh] bg-surface flex items-center overflow-hidden">
      {/* Implementation in pages phase */}
    </section>
  );
}
